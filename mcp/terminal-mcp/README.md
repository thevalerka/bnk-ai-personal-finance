# terminal-mcp

MCP server exposing this platform's own tools (`get_curve`, `get_interest_profile`,
`screen_universe`, `get_briefing`) so the Adaptive Markets Terminal is usable from
Claude Desktop and other MCP clients.

Status: not started. Planned for phase 8 (see `docs/PLAN.md`).
