# packages/shared

Shared TypeScript types, generated from the FastAPI/Pydantic models (via
`datamodel-code-generator` or an OpenAPI → TS step) so the frontend can never
drift from the API. Generation wired up starting phase 1 (Market Data Gateway).
